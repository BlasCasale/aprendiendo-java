package com.mycompany.mavenorgradle;

public class MavenOrGradle {

    public static void main(String[] args) {
        /*
        Maven es una herramienta para gestion y construccion de proyectos de java
        que se caracteriza por tener un modelo de configuracion simple,
        basado en el formato XML
        
        Maven utiliza POM.xml (project object model) para dentro
        el escificar las diferentes dependencias o librerias que seran
        necesarias incluir en el proyecto que se esta desarrollando
       
        Gradle es similar a Maven
        Usa DSL (lenguaje especifico de dominio)
        */
    }
}
